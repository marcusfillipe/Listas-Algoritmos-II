#include <stdio.h>

int somaDigitos(int n) {
    int soma = 0;
    while (n > 0) {
        soma += n % 10; 
        n = n / 10;     
    }
    return soma;

    if(n<0)
    printf("número inválido");
}
